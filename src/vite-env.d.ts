/// <reference types="vite/client" />

// Stamped in at build time by Vite's `define` (see vite.config.ts): the version
// from package.json and the commit SHA from git. Read through src/version.
declare const __APP_VERSION__: string;
declare const __GIT_SHA__: string;
